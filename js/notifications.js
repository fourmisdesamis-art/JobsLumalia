document.addEventListener('DOMContentLoaded',()=>{const s=getSession(),list=document.getElementById('notificationsList');if(!s){list.innerHTML='<div class="empty">Connectez-vous pour consulter vos notifications.</div>';return}const n=JSON.parse(localStorage.getItem('lumalia_jobs_notifications')||'[]').filter(x=>x.userId===s.id).reverse();if(!n.length){list.innerHTML='<div class="empty">Vous n’avez aucune notification.</div>';return}list.innerHTML=n.map(x=>`<article class="notification"><strong>${x.title}</strong><p>${x.message}</p><small>${new Date(x.createdAt).toLocaleString('fr-FR')}</small></article>`).join('')});
